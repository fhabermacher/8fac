<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- sideloaded app: exemption keeps the relay socket alive in Doze
         until the UnifiedPush/FCM wake-up path replaces it -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- fingerprint-first approval on a locked phone (incoming-call style) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:label="8fac"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Approval UI launched from the request notification -->
        <activity android:name=".ApproveActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <!-- Manual fallback: codes shown locally, relay-down ≠ lockout -->
        <activity android:name=".CodesActivity" android:exported="false" />

        <!-- Widget tap target: scope picker + fingerprint, then finishes -->
        <activity android:name=".ArmActivity" android:exported="false"
            android:excludeFromRecents="true" />

        <!-- Home-screen auto-accept widget -->
        <receiver android:name=".ArmWidget" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/arm_widget_info" />
        </receiver>

        <!-- "Stop" action on the auto-accept countdown notification -->
        <receiver android:name=".AutoAccept$DisarmReceiver"
            android:exported="false" />

        <!-- UnifiedPush wake-up delivery (from the distributor app) -->
        <receiver android:name=".WakeReceiver" android:exported="true">
            <intent-filter>
                <action android:name="org.unifiedpush.android.connector.MESSAGE" />
                <action android:name="org.unifiedpush.android.connector.NEW_ENDPOINT" />
                <action android:name="org.unifiedpush.android.connector.UNREGISTERED" />
                <action android:name="org.unifiedpush.android.connector.REGISTRATION_FAILED" />
            </intent-filter>
        </receiver>

        <!-- Keeps the relay WebSocket alive (prototype transport; swap for
             UnifiedPush/FCM wake-ups in production, see PROTOCOL.md §2) -->
        <service android:name=".RelayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
